---
import Nav from "../components/Nav.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <meta
      name="keywords"
      content="npkill, node_modules, npm, node, javascript, tool, optimization, cli"
    />
    <meta
      property="og:title"
      content="NPKILL - Maintain your development workspace clean."
    />
    <meta
      name="description"
      content="List any node_modules 📦 dir in your system and how heavy they are. You can then select which ones you want to erase to free up space 🧹."
    />
    <meta
      property="og:description"
      content="List any node_modules 📦 dir in your system and how heavy they are. You can then select which ones you want to erase to free up space 🧹."
    />
    <meta property="og:site_name" content="npkill" />
    <meta property="og:url" content="https://npkill.js.org" />
    <meta property="og:type" content="website" />
    <meta
      property="og:image"
      content="https://npkill.js.org/img/og-npkill.webp"
    />
    <meta
      name="google-site-verification"
      content="umq3bDhuCGbYZbwjbAt1ufhgUoxTUHmKCUQZYnWvl_Y"
    />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/png" href="favicon.png" />
    <link rel="apple-touch-icon" type="image/png" href="favicon.png" />
    <title>{title}</title>
  </head>
  <body>
    <Nav />
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }

  html {
    font-family: system-ui, sans-serif;
    background-size: 224px;
    color: #3f3f3f;
    font-size: 20px;
    line-height: 1.6;
  }

  body {
    margin: 0;
    overflow-x: hidden;
  }

  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
    border: 1px solid gray;
    border-radius: 4px;
    padding: 2px 4px;
  }

  kbd {
    background-color: #00000008;
    border: 1px solid #8080804a;
    border-radius: 5px;
    padding: 0 3px;
  }

  .content {
    margin: auto;
    padding: 1rem;
    width: 800px;
    max-width: calc(100% - 2rem);
  }
</style>
